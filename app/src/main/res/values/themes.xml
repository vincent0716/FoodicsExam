<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.FoodicsExam" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.FoodicsExam" parent="Base.Theme.FoodicsExam" />

    <style name="ThemeOverlay.MySwitchCompat" parent="Widget.Material3.CompoundButton.MaterialSwitch">
        <!-- active thumb & track color (30% transparency) -->
        <item name="colorPrimary">@color/btnAddColor</item>

        <!-- inactive thumb color -->
        <item name="colorSwitchThumbNormal">@color/black
        </item>

        <!-- inactive track color (30% transparency) -->
        <item name="android:colorForeground">#ffe299
        </item>
    </style>

</resources>